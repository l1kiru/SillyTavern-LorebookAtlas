name: tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # SillyTavern requires Node 20+; the tests use node:test, available since 18.
          node-version: '20'
      - name: Run tests
        run: node --test test/*.test.js
